CREATE VIEW nombrevista AS 
(consulta sql)

ejemplo:

CREATE VIEW pruebavista AS
SELECT pedidos.id, detalle_pedidos.monto, pedidos.fecha,pedidos.estado
FROM pedidos, detallepedidos
WHERE (pedidos.id=detalle_pedidos.idp)